name = "pdd-cost-query"
main = "src/index.js"
compatibility_date = "2024-01-01"

[vars]
NODE_ENV = "production"

# 敏感配置通过 Cloudflare dashboard 或 wrangler secret 设置
# APP_ID, APP_SECRET, APP_TOKEN, TABLE_ID
# VALID_PASSWORD_1, VALID_PASSWORD_2, VALID_PASSWORD_3

# 可观察性配置 - Workers 日志和跟踪
[observability]
enabled = true
head_sampling_rate = 1
